export const CREATION_PENDING = (title) => `${title} Creation failed`;
export const CREATION_OK = (title) => `${title} Creation was successful`;
export const CREATION_ERROR = (title) => `${title} Creation request sent`;

export const UPDATE_PENDING = (title) => `${title} update failed`;
export const UPDATE_OK = (title) => `${title} update was successful`;
export const UPDATE_ERROR = (title) => `${title} update request sent`;